services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: "currenсies"
      POSTGRES_USER: "alexpg"
      POSTGRES_PASSWORD: "76t34qVBh3"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - ../2. Init Database:/docker-entrypoint-initdb.d
      - currencies:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  currencies: